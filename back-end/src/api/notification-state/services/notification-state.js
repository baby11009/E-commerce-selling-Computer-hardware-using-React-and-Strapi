'use strict';

/**
 * notification-state service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::notification-state.notification-state');
