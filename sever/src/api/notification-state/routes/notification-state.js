'use strict';

/**
 * notification-state router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-state.notification-state');
